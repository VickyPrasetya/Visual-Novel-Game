package game.system;

import javafx.scene.Node;

/**
 * Kelas TransitionSystem bertanggung jawab untuk mengelola efek transisi antar scene (fade, slide, dsb).
 */
public class TransitionSystem {
    /**
     * Melakukan efek fade pada node.
     * @param node Node yang akan di-fade.
     * @param durationMs Durasi dalam milidetik.
     */
    public void fadeTransition(Node node, int durationMs) {
        // Implementasi efek fade
    }

    /**
     * Melakukan efek slide pada node.
     * @param node Node yang akan di-slide.
     * @param durationMs Durasi dalam milidetik.
     */
    public void slideTransition(Node node, int durationMs) {
        // Implementasi efek slide
    }
} 