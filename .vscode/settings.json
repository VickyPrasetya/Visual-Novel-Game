{
    // 1. Memberitahu Java di mana folder sumber kode berada
    "java.project.sourcePaths": ["src"],

    // 2. Memberitahu Java untuk menyertakan SEMUA .jar di folder lib
    "java.project.referencedLibraries": [
        "lib/**/*.jar"
    ]
}
